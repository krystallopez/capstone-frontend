import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Freakshow Collectibles',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
